# v4.12.7 — Threshold packs (Shadow Gate policy)

## Roadmap line
recon_v3 + pnl_attrib → PASS/FAIL + score + violations

## What changed
- Added threshold pack storage + eval storage: `src/storage/sqlite_store_thresholds.py`
- Added pack registry (code-based): `src/thresholds/packs.py`
- Added generic evaluator: `src/thresholds/evaluator.py`
- Added runner: `src/runners/threshold_eval.py`
- Added mechanisms doc: `docs/MECHANISMS_v4.12.7_threshold_packs.md`
- Emits `shadow_snapshots(kind='threshold_eval')`

## Mechanism (short)
- Build a context `{recon, pnl, ids}` from the latest recon_v3 + trade_links
- Evaluate each rule → violations with severity + metric + expected/actual
- Score via weighted penalty → PASS/FAIL

## Run
```bash
python -m src.runners.threshold_eval   --db ./journal/trader.db   --signal-idem-key <KEY>   --pack shadow_gate_v1   --pack-version 1.0   --overwrite
```

## Artifacts
- tables: `threshold_packs`, `signal_threshold_eval`
- snapshots: `threshold_eval`

## Next
- v4.12.8 — report pack (batch summaries)
