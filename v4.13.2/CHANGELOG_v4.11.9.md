# Changelog — v4.11.9
- v4.11.8: added `reconcile_report_plus` (CSV + JSON, worst offenders, per-symbol metrics).
- v4.11.9: added Shadow Gate thresholds:
  - `Thresholds` policy schema
  - scorecard evaluation + reasons
  - optional DB persistence (`reconcile_scorecards` table).
