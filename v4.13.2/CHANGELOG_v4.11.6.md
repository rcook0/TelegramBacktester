# Changelog — v4.11.6
- Added model trace persistence: `model_traces` + `model_trace_events` tables.
- Added TraceStore + schema helpers (`ModelTraceDoc`, `TraceEvent`) and `persist_trace()` API.
- Added demo runner to write traces for an existing signal `idem_key`.
