# Changelog — v4.11.5
- v4.11.4: capture window persisted as `shadow_snapshots(kind=capture_window)` for time alignment.
- v4.11.5: added `symbol_resolution` cache (symbolName<->symbolId, digits, pipPosition/pipSize).
- Runner now accepts `--symbol-map-json` for alias->broker name normalization.
